import { atom } from 'nanostores';

export const titleFilter = atom('');
export const isModalOpen = atom(false);
export const categoryFilter = atom('');
export const serviceFilter = atom('');
export const minRatingFilter = atom('');
export const maxRatingFilter = atom('');
